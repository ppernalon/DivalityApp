import { StyleSheet } from "react-native"

export const powerIconStyle = StyleSheet.create({
    powerImage: {
        height: 27,
        marginHorizontal: 20,
        resizeMode: "contain",    
    }
})