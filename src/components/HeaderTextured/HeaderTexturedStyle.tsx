import { StyleSheet } from "react-native"

export const headerTexturedStyle = StyleSheet.create({
    backgroundHeader:{
        width:'100%',
        height: 70,
        justifyContent: "center",
    }
})