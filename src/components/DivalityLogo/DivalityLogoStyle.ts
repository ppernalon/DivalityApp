import { StyleSheet } from "react-native"

export const divalityLogoStyle = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    logoImage: {
        resizeMode: "contain",
        width: '100%',
        height: 150
    }
})