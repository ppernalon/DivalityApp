import { StyleSheet } from "react-native"

export const colors = {
    blue: "#2A82C5"
}

export const globalStyle = StyleSheet.create({
    appBackground: {
        flex: 1,
        backgroundColor: 'white',
    }
})